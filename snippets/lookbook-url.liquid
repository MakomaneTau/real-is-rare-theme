{%- doc -%}
  Resolves the Lookbook destination for navigation and campaign links.
{%- enddoc -%}
{%- if pages.lookbook.url != blank -%}
  {{- pages.lookbook.url -}}
{%- else -%}
  {{- routes.all_products_collection_url -}}?view=lookbook
{%- endif -%}
